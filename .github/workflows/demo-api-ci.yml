name: demo-api CI

on:
  push:
    branches:
      - rel/dev
    paths:
      - 'apps/demo-api/**'
      - '.github/workflows/demo-api-ci.yml'
      - 'k8s/base/apps/demo-api/**'
  pull_request:
    branches:
      - rel/dev
    paths:
      - 'apps/demo-api/**'
      - '.github/workflows/demo-api-ci.yml'
      - 'k8s/base/apps/demo-api/**'
  workflow_dispatch:

env:
  ACR_LOGIN_SERVER: trenchacrcoredev.azurecr.io
  IMAGE_NAME: demo-api

permissions:
  id-token: write
  contents: write

jobs:
  build-test-scan-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install dependencies
        working-directory: apps/demo-api
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt pytest flake8

      - name: Run lint
        working-directory: apps/demo-api
        run: flake8 . --exclude .venv

      - name: Azure login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: ACR login
        run: az acr login --name trenchacrcoredev

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: ./apps/demo-api
          file: ./apps/demo-api/Dockerfile
          push: true
          tags: |
            ${{ env.ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:${{ github.sha }}

      - name: Trivy image scan
        uses: aquasecurity/trivy-action@0.28.0
        with:
          image-ref: ${{ env.ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:${{ github.sha }}
          severity: CRITICAL,HIGH
          ignore-unfixed: true
          format: table

      - name: Configure Git user
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Install yq
        run: |
          sudo wget -qO /usr/local/bin/yq https://github.com/mikefarah/yq/releases/latest/download/yq_linux_amd64
          sudo chmod +x /usr/local/bin/yq

      - name: Update Kustomize image tag
        env:
          NEW_TAG: ${{ github.sha }}
        run: |
          yq -i '.images[0].newTag = strenv(NEW_TAG)' k8s/overlays/dev/apps/kustomization.yaml
          git diff k8s/overlays/dev/apps/kustomization.yaml

      - name: Commit and push manifest changes
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if git diff --quiet; then
            echo "No manifest changes to commit"
            exit 0
          fi
          git add k8s/overlays/dev/apps/kustomization.yaml
          git commit -m "chore: update demo-api image tag to ${GITHUB_SHA}"
          git push
